## Backblaze B2 credentials (required for uploads)
B2_ACCOUNT_ID=exampleId
B2_APP_KEY=exampleKey
B2_BUCKET=your-bucket-name

## Remote path prefix inside the bucket
B2_PREFIX=proxmox/configs

## Rclone remote name (auto-created if missing)
RCLONE_REMOTE=proxmox-b2

## Retention: keep this many most recent archives in B2
RETENTION_COUNT=4

## Local staging and scratch paths
LOCAL_CACHE=/var/backups/proxmox-b2/cache
LOCAL_ARCHIVE_DIR=/var/backups/proxmox-b2/configs

## Mode selector: configs is current; future modes: disks/full
BACKUP_MODE=configs

## Optional: set to 1 for a dry-run (no upload/delete)
DRY_RUN=0
## If 0, delete local archives after successful upload
KEEP_LOCAL=0
